"user strict"; // treat all JS code as newer version
// alert(2+2) // Showing error since we are using nodejs not the browser
let name ="Arka"
let age = 21
let hello;
let hello1 =null;
let isArka = true
console.log(typeof(isArka)) // returns boolean
console.log(typeof(hello)) // returns undefined
console.log(typeof(hello1)) // returns object

// number -> 2^53
// bigint
// string -> ""
// boolean -> true/false
// null -> standalone value
// undefined -> 